engine-strict=true

# The legacy-peer-deps option is required because @woocommerce/components@12.3.0
# declares a peer dependency on react@^17.0.2, while this project uses react@^18.
# It should be removed once @woocommerce/components drops the React 17 peer requirement.
legacy-peer-deps=true
