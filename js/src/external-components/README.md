This directory stores copies of external components, with unreleased fixes that our codebase relies on.

Each file notes the exact commit at which it was cloned, and the list of fixes it's meant to cover.