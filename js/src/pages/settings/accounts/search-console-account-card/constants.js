/**
 * External dependencies
 */
import { __ } from '@wordpress/i18n';

/**
 * The Search Console account card's description — the same across every connection state, so
 * merchants see consistent copy regardless of which state (connect, connecting, verification,
 * connected, etc.) is currently showing.
 */
export const SEARCH_CONSOLE_DESCRIPTION = __(
	'See how your store performs in Google Search.',
	'google-listings-and-ads'
);
